{
  "name": "@readventure/types",
  "version": "1.0.0",
  "description": "Type definitions shared across ReadVenture packages.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "echo \"No tests in @readventure/types\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@readventure/eslint-config": "workspace:*",
    "@readventure/typescript-config": "workspace:*",
    "eslint": "^8.57.1"
  }
}
