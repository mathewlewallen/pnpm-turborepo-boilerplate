{
  "name": "@readventure/prisma",
  "main": "./index.ts",
  "types": "./index.ts",
  "author": "readventure",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@prisma/client": "4.11.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "devDependencies": {
    "prisma": "4.11.0",
    "@readventure/eslint-config": "workspace:*",
    "@readventure/tsconfig": "workspace:*"
  }
}
